[package]
name = "pair"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
ic-cdk = "0.7"
candid = "0.9"
serde = { version = "1.0", features = ["derive"] }

[profile.release]
opt-level = "z"
lto = true